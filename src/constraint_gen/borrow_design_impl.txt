# Design
## Copyright
<legal>
Pointer Ownership Model (POM) Source Code Release

Copyright 2025 Carnegie Mellon University.

NO WARRANTY. THIS CARNEGIE MELLON UNIVERSITY AND SOFTWARE ENGINEERING
INSTITUTE MATERIAL IS FURNISHED ON AN "AS-IS" BASIS. CARNEGIE MELLON
UNIVERSITY MAKES NO WARRANTIES OF ANY KIND, EITHER EXPRESSED OR
IMPLIED, AS TO ANY MATTER INCLUDING, BUT NOT LIMITED TO, WARRANTY OF
FITNESS FOR PURPOSE OR MERCHANTABILITY, EXCLUSIVITY, OR RESULTS
OBTAINED FROM USE OF THE MATERIAL. CARNEGIE MELLON UNIVERSITY DOES NOT
MAKE ANY WARRANTY OF ANY KIND WITH RESPECT TO FREEDOM FROM PATENT,
TRADEMARK, OR COPYRIGHT INFRINGEMENT.

Licensed under a MIT (SEI)-style license, please see license.txt or
contact permission@sei.cmu.edu for full terms.

[DISTRIBUTION STATEMENT A] This material has been approved for public
release and unlimited distribution.  Please see Copyright notice for
non-US Government use and distribution.

DM25-1262
</legal>

## Design

typedef string cpath_part; 
typedef vector<cpath_part> cpath;
// The first part of a cpath is a variable name or "$func_name::return" or "$func_name::args::$arg_name".
// Each additional part of a cpath is either:
//  - "[0]" (for pointer deref or array indexing) or
//  - ".fieldname" for struct fields.
// We sometimes use the term "base var" to refer to the first part of a cpath,
// and we say that the C-path is 'rooted' at the base var.
//
// IMPORTANT: Unlike C, given a pointer p to a struct, we write "p.field1" to
// denote the address of the field in struct pointed to by p, rather than
// "&(*p).field".  We never write "p[0].field" to denote the value of field;
// instead, we write "p.field[0]" for that, in order to align with the LLVM
// GEP instruction.

// Note that a good responsible pointer cpath becomes a zombie under two cases:
// (1) the pointer is freed or (2) the pointer is transferred to another
// responsible cpath.  We generate constraints below to prohibit a responsible
// pointer from becoming a zombie when there are still outstanding borrows from it.

// Note that, unlike Rust, we do not have `mut` and `responsible` annotations
// on everything from the get-go (the `.pom.yml` specifies them for args,
// locals, and returns, but not for results of subexpressions); thus we
// generate constraints that are valid regardless.  This is why the BorrowInfo
// data doesn't record which cpaths are mut and/or responsible -- we don't know
// it yet.

// We do not support unions or structs that contain pointers.
// We currently do not support recursive data structures such as linked lists.
// The main things that are missing (besides properly handing structs at all)
// are (1) generating constraints and borrow/lifetime info when a pointer to a
// struct is loaded from memory and
// (2) verifying that invariants (e.g., no pointers in the struct are zombies)
// aren’t violated except briefly (e.g., verifying that invariants are always
// satisfied before function calls and at the end of basic blocks).

// We catch pointers to local vars that escape that function, but we currently
// do not support catching pointers to locals inside an interior scope that
// escape the scope of the local but not the scope of the function.
// This is because we operate at the LLVM IR level, and conversion to LLVM IR
// does not preserve this sort of use-after-free error.

// Rust allows different fields of a struct to be borrowed independently.  We
// currently don't support this; borrowing a field of a struct borrows the
// whole struct.  Allowing fields to be borrowed independently will be future work.


struct Borrow {
    cpath to;
    cpath from;
}

struct BorrowInfo {
    set<Borrow> borrows;
    map<string /*base_var*/, bool> is_dyn_exclusive; // Whether only a single borrow exists dynamically for base_var.
    map<Borrow, bool> is_must_borrow; // Whether this borrow is known to definitely be in effect.
}

// Note: is_dyn_exclusive turned out not be necessary.  We don't use it, except as an optimization for checking whether we've already generated constraints.


## is_dyn_exclusive

This is to handle things like:
    
    ```
    int x = 1;
    int y = 2;
    int *p1;
    int *p2;

    if (nondet()) {
        p1 = &x;
        p2 = &y;
    } else {
        p1 = &y;
        p2 = &x;
    }
    *p1 = 3;
    *p2 = 4;
    printf("x=%d, y=%d\n", x, y);
    ```

Statically, p1 may point to x, and p2 may point to x, but there is no execution where both point to x at the same time.  The Rust borrow checker allows this, and so will we.


## When copying a pointer from src to dest (including PomVarStore, phi, load, store, and copying to "func::return" during a return instruction)

- Let src_base be the first C-path part of src.
- If there is an existing borrow from a C-path rooted at src_base, then
  - Set is_dyn_exclusive[src_base] = false.
  - Add constraints "!mut(other_dest)" for each Borrow(to=other_dest, from=other_src) where other_src is rooted at src_base.
  - Add a constraint "!mut(dest)".
  - Note: elsewhere, we add constraints "!mut(x) -> !responsible(x)".
- Add Borrow(to=dest, from=src) to BorrowInfo.  
- Set is_must_borrow[Borrow(to=dest, from=src)] = true.


## GEP for calculating address of struct field

Handle copying the pointer like above, except append ".fieldname" to the source cpath.


## Memory reads

LLVM instruction: `%t = load typ, ptr %p`
Add a constraint that no outstanding borrows rooted from %p are mutable.


## Memory writes

LLVM instruction: `store typ t, ptr %p`
First kill the borrows referring to the memory location about to be overwritten
(see section "Overwriting a memory location holding a borrowed pointer").
Then check that there are no outstanding borrows rooted from %p.


## Memory frees

LLVM instruction: `call ... @free(ptr %p)`
Check that there are no outstanding borrows rooted from %p.


## At beginning of basic block

Pseudocode (mishmash of C++ and Python):

    BorrowInfo curBI; // this is be the borrow info at the start of the current basic block.
    vector<BorrowInfo*> incoming = [borrows_at_end_of_bb[pred_bb] for pred_bb in predecessors(curBasicBlock)];
    curBI.borrows = union(bi->borrows for bi in incoming);
    all_base_vars = union(bi->is_dyn_exclusive.keys() for bi in incoming);
    for (base_var in all_base_vars) {
        curBI.is_dyn_exclusive[base_var] = and(bi->is_dyn_exclusive.get(base_var, default=true) for bi in incoming);
    }
    for (Borrow b in curBI.borrow) {
        curBI.is_must_borrow[b] = and(bi->is_must_borrow.get(b, default=false) for bi in incoming);
    }

## At end of basic block

borrows_at_end_of_bb[&BB] = curBI;


## At function callsite

For every CPath src_formal rooted at "$callee::return" or "$callee::args:$arg_name":
    For every CPath dest_formal rooted at "$callee::return" or "$callee::args:$arg_name":
        Let src_actual and dest_actual be the corresponding CPaths using the actual arguments or actual return value.
        If the lifetime for dest_formal in POM YML model includes src_formal, then add Borrow(to=dest_actual, from=src_actual).

TODO: eventually compute transitivity, to alleviate burden of redundant annotations;
for now, require explicit listings, and generate a failed constraint if any are missing
(see below section "At function exit point").


## At function exit point

Check that borrows among formal params and formal "::return" are captured in the provided POM model.
Check that no stack-allocated locals (`alloca`) escape the function (i.e., they shouldn't be borrowed by any cpath rooted at the function parameters or return value).


## When a variable becomes dead

(Note: function args are artificially kept alive thru the end of the function.)

Example 1:
    p2.field2[0].field3[0] = p1.field1; // borrows_of(p1.field1) = {p2.field2[0].field3[0]}
    p3[0] = p2.field2;                  // borrows_of(p2.field2) = {p3[0]}
    p2 = NULL;                          // borrows_of(p1.field1) = {p3[0][0].field3[0]}

    // Del: Borrow(to=("p2",".field2","[0]",".field3","[0]"), from=("p1", ".field1"))
    // Del: Borrow(to=("p3", "[0]"),                          from=("p2", ".field2"))
    // Add: Borrow(to=("p3", "[0]", "[0]", ".field3", "[0]"), from=("p1", ".field1"))
    
When a variable becomes dead:
    if  Borrow(to=(dead_var + suffixA), from=(varB + suffixB))
    and Borrow(to=(varC + suffixC),     from=(dead_var + suffixD))
    and suffixA.startswith(suffixD), then
    add Borrow(to=(varC + suffixC + suffixA.remove_prefix(suffixD)), from=(varB + suffixB))
    

Example 2:
    p2.field2[0] = p1.field1;       // borrows_of(p1.field1) = {p2.field2[0]}
    p3[0] = p2.field2[0][0].field3; // borrows_of(p2.field2[0][0].field3) = {p3[0]}
    p2 = NULL;                      // borrows_of(p1.field1[0].field3) = {p3[0]}

When a variable becomes dead:
    if  Borrow(to=(dead_var + suffixA), from=(varB + suffixB))
    and Borrow(to=(varC + suffixC),     from=(dead_var + suffixD))
    and suffixD.startswith(suffixA), then
    add Borrow(to=(varC + suffixC), from=(varB + suffixB + suffixD.remove_prefix(suffixA)))


Example 3:
    borrow_to[temp] = {p3[0]}
    borrow_to[temp[0]] = {p1}
    // temp dies
    borrow_to[p3[0][0]] = {p1}

When a variable becomes dead:
    if  Borrow(to=(dead_var + suffixA), from=(varB + suffixB))
    and Borrow(to=(dead_var + suffixD), from=(varC + suffixC))
    and suffixD.startswith(suffixA), then
    add Borrow(to=(varB + suffixB + suffixD.remove_prefix(suffixA)), from=(varC + suffixC))


Example 4:
    temp = malloc(...);
    p1 = temp;
    p2 = temp;
    /* temp is killed here because there are no more uses of it */
    ...

Given an LLVM instruction "%x = alloca ...", when x becomes dead and borrows_from_dead.size() > 0:
    In this case, we do not make any changes to the BorrowInfo.  Instead, we
    let the dead variable hang around.  The reason for this to enable detecting
    when the address returned by alloca outlives the function.

After splicing the borrow chains as described above, we delete
every Borrow(to=dest, from=src) where dest or src is a suffix of dead_var.
(Also, when a borrow is deleted, also delete the is_must_borrow entry for the deleted borrow.)

When a borrow is created by splicing together two borrows above, set:
is_must_borrow[new_borrow] = is_must_borrow[old_borrow_1] && is_must_borrow[old_borrow_2]

For every base var bv: if deleting borrows above reduces the number of borrows
rooted at bv to 1, then set is_dyn_exclusive[bv] = true.  If the number of
borrows is reduced to 0, then call is_dyn_exclusive.erase(bv).

Note: the splicing here is needed to create function summaries and verify that
they are consistent with the provided ".pom.yml" model (see the section
"At function exit point").  In particular, we need to identify borrows involving
the function arguments and return value that escape the function.


## Overwriting a memory location holding a borrowed pointer

This is handled the same way as when a variable becomes dead, with the dead cpath instead of dead_var.
However, definite location aliases (is_must_borrow=true) of the memory location should be processed too.


## Pointer depth

Given a C-path P, we write `ptr_depth(P)` to indicate the pointer depth of P.  For example:
 - If the type of P is `int`, then ptr_depth(P)=0
 - If the type of P is `int*`, then ptr_depth(P)=1
 - If the type of P is `int**`, then ptr_depth(P)=2


## Updating aliases

For soundness, we only need to consider aliases of a pointer eligible to be used for writing (i.e., pointer must be mutable and must have no outstanding borrows).

If there is a borrow from p1 to p2, then ptr_aliases_of(p2) includes p1 and all of ptr_aliases_of(p1).
If there is a borrow from p1 to p2, then ptr_aliases_of(*p2) includes *p1 and all of ptr_aliases_of(*p1).
If there is a borrow from p1 to p2, then ptr_aliases_of(p2.fld) includes p1.fld and all of ptr_aliases_of(p1.fld).


## Transitive borrows

We write "borrow+" as an abbreviation of "transitive borrow".
For MAY_OR_MUST in [MAY, MUST]:
 - If there is a direct borrow from p1 to p2, then there is a MAY_OR_MUST borrow+ from p1 to p2.
 - If there is a MAY_OR_MUST borrow+ from p1 to p2 and a MAY_OR_MUST borrow+ from p2 to p3, then there is a MAY_OR_MUST borrow+ from p1 to p3.
 - If there is a MAY_OR_MUST borrow+ from p1 to p2, and ptr_depth(p1) >= 2 and ptr_depth(p2) >= 2, then there is a MAY_OR_MUST borrow+ from *p1 to *p2.
 - If there is a MAY_OR_MUST borrow+ from p1 to p2, and p1 and p2 are pointers to struct with a field `fld`, then there is a MAY_OR_MUST borrow+ from p1.fld to p2.fld.



## New Borrow Info

struct BorrowInfo {
    set<Borrow> may_trans_borrows;
    set<Borrow> must_trans_borrows;
}

define bi::may_trans_borrows_to(p)  = [borrow.from for borrow in this.may_trans_borrows  if borrow.to == p]
define bi::must_trans_borrows_to(p) = [borrow.from for borrow in this.must_trans_borrows if borrow.to == p]

define bi::may_trans_borrows_from(p)  = [borrow.to for borrow in this.may_trans_borrows  if borrow.from == p]
define bi::must_trans_borrows_from(p) = [borrow.to for borrow in this.must_trans_borrows if borrow.from == p]


## Aliases

A pointer is *eligible to be used for writing* if it is mutable and has no outstanding borrows.

If a pointer p is eligible to be used for writing and no mut constraints are violated, then
may_ptr_aliases(p) = may_trans_borrows_to(p)
may_loc_aliases(*p) = [*x for x in may_trans_borrows_to(p)]
may_loc_aliases(v) = [v] if v is an SSA value
